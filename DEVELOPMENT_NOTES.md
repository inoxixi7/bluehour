# 开发注意事项 ⚠️

## 当前状态

✅ **已完成**:
- 项目结构搭建
- 所有核心文件创建
- TypeScript 配置
- 导航架构
- 三个计算器功能逻辑
- 日出日落 API 集成
- 基础 UI 组件

⏳ **待完成**:
- 依赖安装 (`npm install`)
- 占位图片创建
- Firebase 配置（可选）
- 实际设备测试

## 首次运行步骤

### 1. 安装依赖
```bash
npm install
```

这将安装所有必需的包，包括：
- React Native & Expo
- React Navigation
- TypeScript
- Expo Location
- 等等...

### 2. 创建占位图片

**选项 A: 使用脚本（需要 ImageMagick）**
```bash
# 安装 ImageMagick
brew install imagemagick

# 运行脚本
chmod +x create-placeholder-images.sh
./create-placeholder-images.sh
```

**选项 B: 手动创建**
在 `assets/images/` 目录下创建以下纯色图片：
- `icon.png` (1024x1024, 蓝色)
- `splash.png` (1242x2436, 深蓝色)
- `adaptive-icon.png` (1024x1024, 蓝色)
- `favicon.png` (48x48, 蓝色)

**选项 C: 使用在线工具**
访问 https://icon.kitchen 快速生成图标集

### 3. 启动开发服务器
```bash
npm start
```

### 4. 在设备上测试
- 扫描二维码（Expo Go 应用）
- 或按 `i` (iOS) / `a` (Android)

## TypeScript 编译错误

当前显示的 TypeScript 错误（找不到模块）是**正常的**，因为：
1. 依赖还未安装
2. 运行 `npm install` 后会自动解决

不要担心这些错误！

## 已知限制和待解决问题

### 1. Picker 组件
使用了 `@react-native-picker/picker`，需要在安装依赖时确保包含。

如果遇到问题，可以运行：
```bash
npx expo install @react-native-picker/picker
```

### 2. 位置权限
在 iOS 和 Android 上需要用户授予位置权限。首次运行时会请求权限。

**iOS 模拟器测试位置**:
- Features → Location → Custom Location
- 输入纬度和经度

**Android 模拟器测试位置**:
- Extended Controls (⋯) → Location
- 输入坐标

### 3. API 请求
sunrise-sunset.org API 是免费的，但有速率限制。如果遇到错误，可能需要等待几分钟。

### 4. Babel Module Resolver
`babel.config.js` 中配置了 `@` 别名指向 `src` 目录。

如果遇到别名解析问题：
```bash
npm start -- --clear
```

## 推荐的开发顺序

### 阶段 1: 基础运行 ✅
1. ✅ 项目结构已创建
2. 安装依赖
3. 创建占位图片
4. 启动并查看应用

### 阶段 2: 功能测试
1. 测试底部导航切换
2. 测试计算器标签切换
3. 测试 EV 计算器逻辑
4. 测试 ND 滤镜计算器
5. 测试景深计算器

### 阶段 3: 日出日落功能
1. 测试位置权限请求
2. 测试 API 调用
3. 验证时间计算准确性

### 阶段 4: UI 优化
1. 替换 emoji 图标为 vector icons
2. 优化颜色和布局
3. 添加动画效果
4. 响应式设计调整

### 阶段 5: 高级功能
1. 实现地图选点
2. 添加日期选择器
3. 集成 Firebase
4. 实现收藏功能

## 调试技巧

### 查看日志
```bash
# 在开发服务器运行时按
j  # 打开 Chrome DevTools
r  # 重新加载应用
```

### 清除缓存
```bash
npm start -- --clear
```

### 重置 Metro bundler
```bash
rm -rf node_modules
npm install
npm start -- --clear
```

### 检查端口占用
```bash
# 如果 19000 端口被占用
lsof -ti:19000 | xargs kill
```

## 性能注意事项

### 1. 图片优化
- 使用合适尺寸的图片
- 考虑使用 WebP 格式
- 启用图片缓存

### 2. 列表渲染
如果未来添加列表功能，使用 `FlatList` 而非 `ScrollView`：
```typescript
<FlatList
  data={items}
  renderItem={({ item }) => <Item {...item} />}
  keyExtractor={item => item.id}
/>
```

### 3. 状态管理
当应用复杂度增加时，考虑使用：
- Context API（轻量级状态）
- Redux Toolkit（复杂状态）
- Zustand（简单高效）

## 代码质量工具（可选）

### ESLint
```bash
npm install --save-dev eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
```

### Prettier
```bash
npm install --save-dev prettier eslint-config-prettier
```

### Husky (Git hooks)
```bash
npm install --save-dev husky lint-staged
```

## 部署前检查清单

- [ ] 所有计算器逻辑经过测试
- [ ] 位置权限正常工作
- [ ] API 调用处理错误情况
- [ ] 创建正式的应用图标
- [ ] 配置应用名称和包名
- [ ] 测试 iOS 和 Android 两个平台
- [ ] 优化图片和资源大小
- [ ] 添加错误边界
- [ ] 实现错误追踪（Sentry）
- [ ] 准备隐私政策和服务条款

## 常见错误及解决方案

### "Unable to resolve module"
```bash
npm install
npm start -- --clear
```

### "Metro bundler is not running"
```bash
# 完全重启
rm -rf node_modules
npm install
npm start
```

### "Expo Go version mismatch"
更新 Expo Go 应用到最新版本

### "Location permission denied"
检查 `app.json` 中的权限配置

## 获取帮助

如果遇到问题：

1. **查看日志**: 终端和 Expo DevTools
2. **搜索错误**: Google + Stack Overflow
3. **官方文档**:
   - [Expo 文档](https://docs.expo.dev/)
   - [React Navigation](https://reactnavigation.org/)
   - [React Native](https://reactnative.dev/)
4. **社区**:
   - [Expo Forums](https://forums.expo.dev/)
   - [React Native Discord](https://discord.com/invite/react-native)

## 下一个里程碑 🎯

1. **完成基础测试**: 确保所有计算器功能正常
2. **UI 美化**: 使用专业图标和动画
3. **添加地图功能**: 实现地点选择
4. **Firebase 集成**: 用户数据持久化
5. **发布 Beta 版**: 收集用户反馈

---

**记住**: 这是一个活跃开发中的项目。遇到问题是正常的，一步一步来！

祝编码愉快！🚀
