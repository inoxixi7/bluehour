# 快速开始指南 🚀

## 第一步：环境准备

### 1. 安装 Node.js
确保已安装 Node.js 18 或更高版本：
```bash
node --version
```

### 2. 安装 Expo CLI
```bash
npm install -g expo-cli
```

### 3. 安装 Expo Go 应用
- **iOS**: 从 App Store 下载 "Expo Go"
- **Android**: 从 Google Play 下载 "Expo Go"

## 第二步：项目设置

### 1. 安装依赖
```bash
cd bluehournew
npm install
```

### 2. 创建占位图片（临时）
在开发阶段，需要创建基本的图片资源。可以使用以下命令生成：

```bash
# 创建简单的占位图片（macOS/Linux）
# 如果没有 imagemagick，请先安装：brew install imagemagick

convert -size 1024x1024 xc:#3498db assets/images/icon.png
convert -size 1242x2436 xc:#1a1a2e assets/images/splash.png
convert -size 1024x1024 xc:#3498db assets/images/adaptive-icon.png
convert -size 48x48 xc:#3498db assets/images/favicon.png
```

或者手动创建纯色图片文件，尺寸如下：
- `icon.png`: 1024x1024
- `splash.png`: 1242x2436
- `adaptive-icon.png`: 1024x1024
- `favicon.png`: 48x48

## 第三步：启动应用

### 1. 启动开发服务器
```bash
npm start
```

### 2. 在设备上运行
- 扫描终端中的二维码（使用 Expo Go 应用）
- 或按 `i`（iOS 模拟器）/ `a`（Android 模拟器）

## 项目功能测试

### 测试蓝调时刻规划器
1. 打开应用
2. 点击底部 "蓝调时刻" 标签
3. 点击 "获取当前位置" 按钮
4. 允许位置权限
5. 查看日出日落时间

### 测试 EV 计算器
1. 点击底部 "计算器" 标签
2. 选择 "EV 曝光" 标签
3. 调整光圈、快门或 ISO
4. 选择要锁定的参数
5. 点击 "计算等效曝光"

### 测试 ND 滤镜计算器
1. 在计算器中选择 "ND 滤镜" 标签
2. 选择基础快门速度
3. 选择 ND 滤镜型号
4. 点击 "计算新快门速度"
5. 如果快门 ≥1 秒，可以启动计时器

### 测试景深计算器
1. 在计算器中选择 "景深" 标签
2. 选择传感器类型
3. 设置焦距、光圈和对焦距离
4. 点击 "计算景深"
5. 查看清晰范围和超焦距

## 常见问题

### 1. Metro bundler 错误
```bash
# 清除缓存
npm start -- --clear
```

### 2. iOS 模拟器无法打开
```bash
# 确保已安装 Xcode
xcode-select --install
```

### 3. Android 模拟器无法连接
```bash
# 检查 adb 连接
adb devices
```

### 4. 位置权限不工作
- 在模拟器中，需要手动设置模拟位置
- iOS: Features -> Location -> Custom Location
- Android: Extended Controls -> Location

### 5. TypeScript 错误
目前显示的编译错误是因为还没有安装依赖。运行 `npm install` 后会自动解决。

## 下一步

### 配置 Firebase（可选）
1. 创建 Firebase 项目
2. 下载配置文件
3. 创建 `src/config/firebase.ts`
4. 初始化 Firebase SDK

### 添加图标库
建议安装 `@expo/vector-icons`：
```bash
npx expo install @expo/vector-icons
```

然后在导航中替换 emoji 图标为专业图标。

### 部署应用

#### 发布到 Expo
```bash
expo publish
```

#### 构建独立应用
```bash
# iOS
eas build --platform ios

# Android
eas build --platform android
```

## 开发建议

1. **先测试 Web 版本**：在浏览器中快速预览
   ```bash
   npm start
   # 按 'w' 键
   ```

2. **使用热重载**：修改代码后自动刷新

3. **查看日志**：按 `j` 打开开发者工具

4. **性能优化**：使用 React DevTools Profiler

## 获取帮助

- [Expo 文档](https://docs.expo.dev/)
- [React Navigation 文档](https://reactnavigation.org/)
- [React Native 文档](https://reactnative.dev/)

祝开发愉快！🎉
